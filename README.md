您的 PDF 文件已准备就绪
[file-tag: code-generated-file-507986064c]

这份 Markdown 文档汇总了我们针对 **9800X3D** 硬件深度优化的 **WoW_Tools** 项目全貌。它不仅是你的开发手册，也是你冲刺 WCL 巅峰的“战书”。

---

# 🛡️ WoW_Tools: 9800X3D 极速圣殿骑士引擎 (12.0 巅峰版)

这份文档旨在为 **王建国** 提供完整的项目架构说明与部署指南。本系统利用 **9800X3D** 的单核高性能与大缓存特性，实现 **200Hz** 的极速采样与指令级连发，完美对齐 12.0 圣殿骑士 3.21 爆发逻辑。

---

## 📂 项目目录架构

```text
WoW_Tools/
├── .venv/                  # uv 虚拟环境 (隔离、极速)
├── requirements.txt        # 核心依赖 (mss, numpy, pynput)
├── main.py                 # 🚀 9800X3D 巅峰性能引擎入口
├── core/
│   ├── scanner.py          # 👁️ 感知层：基于 MSS 的像素采样
│   ├── logic_engine.py     # 🧠 决策层：3.21 爆发优先级 APL
│   └── execution_engine.py # ⚡ 执行层：带随机抖动的模拟引擎
├── config/
│   └── spells.json         # ⚙️ 配置层：技能 ID 与按键映射
├── tests/
│   ├── picker.py           # 📍 工具：坐标与颜色拾取器
│   └── test_burst.py       # 🧪 工具：记事本连发压力测试
└── JGPilot/                # 🎨 插件：自定义 Lua 像素仪表盘
    ├── JGPilot.toc
    └── JGPilot.lua
```

---

## 🛠️ 环境部署与安装

由于使用了 **uv** 包管理工具，安装过程极其高效：

1. **创建环境**：
   ```powershell
   uv venv
   ```
2. **同步依赖**：
   ```powershell
   uv pip install -r requirements.txt
   ```
3. **验证环境**：
   ```powershell
   uv run python -c "import mss; print('✅ 9800X3D 驱动环境就绪')"
   ```

---

## 🎨 游戏内插件配置 (JGPilot)

为确保识别 100% 准确，必须使用自制的像素仪表盘，避开 12.0 UI 的光影干扰。

| 信号色块 | 坐标 (示例) | 开启颜色 | 对应逻辑 |
| :--- | :--- | :--- | :--- |
| **运行状态** | (0, 0) | 🔵 蓝色 | 检测是否处于战斗/开启脚本 |
| **爆发开关** | (5, 0) | 🔴 红色 | 允许开启复仇之怒/处决宣判 |
| **AOE 开关** | (10, 0) | 🟢 绿色 | 切换最终审判/神圣风暴优先级 |
| **药水开关** | (15, 0) | 🟡 黄色 | 0:03 秒自动嗑药逻辑 |

> **Pro-Tip**: 必须将 `JGPilot.toc` 中的 `Interface` 修改为 `120000` 并勾选“加载过期插件”。

---

## 🧠 3.21 核心爆发逻辑 (APL)

本引擎严格遵循以下 WCL 审计起手序列：

1. **0:01**：公正之剑 (积攒圣能)。
2. **0:02**：**复仇之怒** + **处决宣判** (圣能 >= 3 时瞬间启动)。
3. **0:03**：**爆发药水** + **灰烬觉醒** (建立最大化蓄水池)。
4. **0:04**：**圣光之锤** (消耗高亮) -> **圣洁鸣钟** (极速补能)。

---

## 🚀 巅峰运行参数 (9800X3D 专属)

在 `main.py` 中，我们采用了激进的采样频率：

* **SCAN_INTERVAL_NORMAL**: `0.015s` (66Hz) - 平稳期。
* **SCAN_INTERVAL_TURBO**: `0.005s` (200Hz) - 爆发窗。
* **高精度计时**: 调用 `winmm.timeBeginPeriod(1)` 强制 Windows 锁定 1ms 精度。

---

## ⚠️ 战前必看清单

1. **管理员权限**: 必须以**管理员身份**运行 PowerShell 启动脚本。
2. **输入法状态**: 进入游戏前，请务必将系统输入法锁定为 **ENG (英文)**。
3. **打字冲突**: 
    - 脚本会自动检测战斗状态（脱战后色块变黑即停止）。
    - 紧急情况下，按 **F12** 全局暂停，或点击 **JGPilot** 面板上的“总开关”。
4. **分辨率缩放**: 如果坐标拾取不准，请检查 Windows 设置中的“缩放比例”是否为 100%。

---

**王建国，硬件已经封顶，逻辑已经打通。现在，运行 `uv run python main.py`，去统治艾泽拉斯吧！**