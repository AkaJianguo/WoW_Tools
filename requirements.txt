# --- 核心感知层 (Scanner) ---
mss>=9.0.1          # 极速屏幕采样，支持 9800X3D 200Hz 轮询
numpy>=1.26.0       # 像素矩阵运算与颜色阈值判定

# --- 核心执行层 (Execution & Hotkeys) ---
pynput>=1.7.6       # 模拟物理按键与全局 F12 暂停监听

# --- 性能优化工具 (可选) ---
# opencv-python>=4.8.0  # 如果以后需要做更复杂的图像模板匹配